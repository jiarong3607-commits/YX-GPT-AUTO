---
title: "gpt-auto 共享任务台账与需求流转规范"
version: "2.0"
updated: "2026-09-05"
project: "gpt-auto"
language: "zh-CN"
audience:
  - "公司内部项目负责人"
  - "产品助理 AI"
  - "项目中控 AI"
  - "任务协调 AI"
  - "开发 / 审核 / 发布 / 运维 AI"
purpose:
  - "让多个 gpt-auto 需求可以并行推进"
  - "让负责人不再充当多个 AI 窗口之间的传话人"
  - "让 Obsidian、GitHub、Orca 共同形成长期项目记忆"
  - "让新 AI 或内部开发人员快速进入项目"
  - "让账号池相关需求、开发、审核和上线过程可追踪"
core_model: "一件事一个任务文件；一份文件一个主要维护者；中控统一读取；开发以 GitHub 为准"
project_scope: "公司内部使用的 GPT 账号池与相关管理能力"
out_of_scope: "广告、支付、积分、订阅、定价、营销增长、公开用户注册等商业化功能"
dynamic_facts_notice: "PR、部署、账号状态、配额、权限、运行数据等动态事实，执行前必须重新核验，不能只依赖历史笔记。"
---

# gpt-auto 共享任务台账与需求流转规范 2.0
## AI 可读｜非技术负责人可懂｜可复制话术

> 本文用于 **gpt-auto 公司内部 GPT 账号池项目** 的需求流转、任务协作和项目记忆管理。
>
> 核心目标不是把所有事情塞给一个 AI，而是让产品讨论、任务协调、开发、审核、发布和运维各自负责自己的部分，同时通过统一任务台账保持信息一致。

---

# 0. 先明确项目边界

## 0.1 gpt-auto 是什么

当前按以下定位管理：

> **gpt-auto 是公司内部使用的 GPT 账号池及其配套管理项目。**

本项目的任务通常会围绕：

- GPT 账号池管理；
- 账号状态与可用性；
- 账号分配、回收与调度；
- 账号异常处理；
- 使用状态、配额或容量信息；
- 内部权限控制；
- 管理后台；
- 日志、审计与运行监控；
- 自动化任务；
- 稳定性、测试、部署和运维。

具体功能以正式需求文件和 GitHub 任务为准，不因为本文举例而自动进入开发范围。

## 0.2 明确不做什么

本项目仅供公司内部使用，当前不考虑以下能力：

```text
广告
支付
积分
订阅
定价 / 套餐
营销增长
公开用户注册
面向外部用户的商业化流程
```

后续任何 AI 在整理需求时，如果发现讨论偏向这些方向，应先标记为“当前项目范围外”，不要顺手加入 gpt-auto。

## 0.3 一句话理解这套协作方式

> **Obsidian 保存“为什么做、做到哪、做过什么决定”；GitHub 保存“具体怎么开发、代码和版本是什么”；项目中控负责统一读取、排期和派单；每个长期任务由自己的协调窗口维护。**

负责人最终只需要：

```text
告诉 AI 想解决什么问题
↓
产品助理或任务协调 AI 帮忙收敛需求
↓
中控安排优先级、依赖和执行顺序
↓
开发 / 审核 / 发布 AI 各自完成自己的工作
↓
只有真正需要负责人决定的事情再回来找负责人
```

---

# 1. 为什么要这样设计

常见的失控方式是：

```text
一个中控窗口
├─ 聊需求
├─ 查代码
├─ 查账号状态
├─ 排查 Bug
├─ 派开发
├─ 做审核
├─ 等 CI
├─ 写项目笔记
├─ 跟部署
└─ 等其他 Agent
```

最终会出现：

- 中控长时间被一个任务占住；
- 中控开始替代开发或审核 AI；
- 同一个需求出现多个版本；
- 多个开发分支彼此冲突；
- 账号池规则只存在聊天里，没有稳定记录；
- 负责人不断复制消息，变成人肉传话员；
- 新 AI 接手时要重新听完整背景；
- 不使用某个具体 AI 工具后，项目记忆就断掉；
- 账号、权限、部署等动态事实被旧笔记误导。

所以 gpt-auto 不使用“万能中控”模式。

---

# 2. 最终架构：项目记忆 + 工程真相 + 执行现场

```text
                              负责人
              ┌────────────────┼────────────────┐
              ↓                ↓                ↓
       💡 产品助理        各主题任务协调窗口      🎛 项目中控
       模糊需求与方案       账号池 / 权限 / Bug      排期与调度
              │                │                │
              └───────┬────────┴────────┬───────┘
                      ↓                 ↓
                 Obsidian 共享任务台账
            一件事一个文件，一个文件一个维护者
                      │
                      └──────── 桥梁 ────────┐
                                             ↓
                                           GitHub
                            正式任务 / Issue / PR / 代码 / 审核
                                             ↓
                          ┌──────────────────┼──────────────────┐
                          ↓                  ↓                  ↓
                       开发 AI            审核 AI         发布 / 运维 AI
```

## 2.1 三个系统分别负责什么

### Obsidian

```text
= 产品和项目记忆
= 为什么做
= 当前做到哪
= 已经确认过什么
= 还有什么没决定
= 任务之间是什么关系
```

### GitHub

```text
= 工程记忆和开发真相
= 正式开发任务
= 代码
= Issue
= PR
= Review
= 测试
= 版本
= 技术风险
```

### Orca 或当前 AI 执行环境

```text
= 当前执行现场
= 哪个窗口在做什么
= 哪个 Agent 正在工作
= 哪个任务正在等待
```

Orca 是执行工具，不是唯一项目记忆。

## 2.2 Obsidian 和 GitHub 怎么衔接

需求成熟后：

```text
Obsidian 中的正式需求
↓
项目中控提取开发真正需要的信息
↓
建立或更新 GitHub 正式任务
↓
开发 AI 按仓库规则定位代码
```

开发结束后：

```text
GitHub PR / Review / 发布结果
↓
任务协调 AI 提炼重要事实
↓
更新对应 Obsidian 任务文件
```

不要把完整代码日志、CI 日志或聊天记录复制回 Obsidian。

---

# 3. 最重要的写入规则

## 3.1 不是所有 AI 都改一个“项目总表”

错误：

```text
账号池 AI ─┐
权限 AI    ├─→ 项目总表.md
Bug AI     ┤
中控       ┘
```

这样容易互相覆盖、重复记录和产生冲突。

正确：

```text
产品助理
→ 只维护产品助理区域

账号池协调 AI
→ 账号池核心管理.md

权限协调 AI
→ 内部权限与角色.md

稳定性协调 AI
→ 账号可用性与异常恢复.md

项目中控
→ 主要读取所有任务文件
→ 只维护项目看板和“需要负责人决定”
```

## 3.2 一件事一个文件

例如：

```text
任务台账/
├─ 账号池核心管理.md
├─ 账号分配与回收.md
├─ 账号状态同步.md
├─ 内部权限与角色.md
├─ 管理后台.md
├─ 账号异常恢复.md
└─ 运行监控与告警.md
```

## 3.3 一个文件一个主要维护者

| 文件或区域 | 主要维护者 |
|---|---|
| `产品助理/**` | 产品助理 |
| `任务台账/账号池核心管理.md` | 对应任务协调窗口 |
| `任务台账/账号分配与回收.md` | 对应任务协调窗口 |
| `任务台账/内部权限与角色.md` | 对应任务协调窗口 |
| `项目中控/**` | 项目中控 |
| GitHub Issue / PR | 对应开发、审核和项目中控 |
| 全局产品决定 | 产品助理或知识同步角色，经负责人确认 |

## 3.4 开发 Worker 不直接维护全局 Obsidian

开发 AI 主要负责：

- 代码；
- 测试；
- GitHub Issue；
- PR；
- Review 修复；
- 当前版本；
- 技术风险；
- 必要的工程文档。

普通开发 AI 不要求访问负责人的全部 Obsidian。

开发过程中发现新需求时：

```text
开发 AI
→ 在 GitHub / Handoff 中提出“新需求候选”
→ 当前任务协调窗口记录
→ 产品助理或中控决定是否进入正式需求
```

不要让开发 AI 顺手扩大范围。

---

# 4. 推荐的 Obsidian 目录

不需要一次整理全部旧笔记，先建立 gpt-auto 的稳定入口：

```text
gpt-auto/
├─ AI项目记忆入口.md
├─ 当前项目目标.md
│
├─ 产品助理/
│  ├─ 00-使用说明.md
│  ├─ 01-当前重点.md
│  ├─ 02-想法池.md
│  ├─ 03-待中控安排.md
│  └─ 需求/
│
├─ 任务台账/
│  ├─ 账号池核心管理.md
│  ├─ 账号分配与回收.md
│  ├─ 账号状态同步.md
│  ├─ 内部权限与角色.md
│  ├─ 管理后台.md
│  ├─ 账号异常恢复.md
│  └─ ...
│
├─ 项目中控/
│  ├─ 项目看板.md
│  ├─ 需要负责人决定.md
│  └─ 最近更新.md
│
├─ 产品决策/
├─ SOP/
└─ 归档/
```

## 旧文件怎么办

```text
不删除
不大规模移动
不一次性整理
```

以后真正用到旧资料时，再把有价值的结论吸收到 gpt-auto 新结构。

---

# 5. 每个任务文件的标准格式

> 负责人不需要手动填写。把模板交给对应的任务协调 AI 维护即可。

```markdown
---
task_name: ""
status: "想法 / 调研 / 待安排 / 进行中 / 等待 / 审核 / 待上线 / 已完成"
priority: "高 / 中 / 低"
risk: "低 / 普通 / 高"
owner_window: ""
last_updated: ""
needs_owner_decision: false
github_issue: ""
github_pr: ""
current_version: ""
---

# 任务名称

## 这件事是做什么的

用非技术人员也能看懂的话说明。

## 为什么现在做

说明和当前项目目标的关系。

## 本次范围

- 

## 本次明确不做

- 

## 已经完成

- 

## 还没有完成

- 

## 现在为什么停在这里

- 

## 下一步

只保留一个最重要的下一步。

## 需要负责人决定

没有则写“没有”。

## 已确认决定

- 

## 还没确认

- 

## 与其他任务的关系

- 依赖：
- 可能冲突：
- 可以并行：

## GitHub 工程入口

- Issue：
- PR：
- 最新版本：
- 审核结果：
- 发布状态：

## 新发现但不属于本任务的需求

只记录候选，不在当前任务顺手开发。

## 长期需要沉淀的内容

- 产品决定：
- 账号规则：
- 权限规则：
- SOP：
- 数据口径：
- 事故复盘：
```

---

# 6. gpt-auto 建议拆成哪些长期主题

不是现在必须一次全部创建，而是需要时按主题建任务文件。

## 6.1 账号池核心管理

可能包括：

- 账号录入；
- 账号启用 / 停用；
- 账号池分组；
- 可用 / 不可用状态；
- 备注与内部标签；
- 账号生命周期。

## 6.2 账号分配与回收

可能包括：

- 什么情况下分配账号；
- 如何选择可用账号；
- 使用结束后如何回收；
- 是否需要自动轮换；
- 并发冲突怎么处理。

是否自动分配、轮换以及具体规则，必须以正式需求为准。

## 6.3 账号状态与异常恢复

可能包括：

- 登录失效；
- 账号不可用；
- 配额或容量异常；
- 状态同步失败；
- 自动重试；
- 人工介入入口。

## 6.4 内部权限

可能包括：

- 谁能查看账号；
- 谁能分配账号；
- 谁能修改配置；
- 谁能执行高风险操作；
- 谁能查看日志。

## 6.5 管理后台

只做公司内部需要的管理能力，不按公开 SaaS 产品设计。

## 6.6 监控与审计

可能包括：

- 账号池可用数量；
- 异常账号；
- 分配失败；
- 长时间占用；
- 自动任务失败；
- 关键操作记录。

---

# 7. 五类 AI 怎样分工

## 7.1 产品助理

适合处理：

- 模糊想法；
- 账号池使用流程；
- 管理后台需求；
- 内部角色和权限需求；
- 自动化想法；
- 操作体验；
- Bug 反馈中可能隐藏的新需求。

负责：

```text
想法
↓
合并和澄清
↓
区分现在做 / 以后做 / 不做
↓
形成正式需求
↓
写入产品助理区域
```

不负责开发和实时项目执行状态。

## 7.2 任务协调窗口

每个复杂或长期主题可以有一个长期协调窗口，例如：

```text
账号池核心管理｜任务协调
账号分配与回收｜任务协调
内部权限｜任务协调
账号异常恢复｜任务协调
```

负责：

- 保存本任务背景；
- 记录已经确认的决定；
- 管理本任务子阶段；
- 读取相关 GitHub Issue / PR 当前状态；
- 更新自己的 Obsidian 任务文件；
- 向中控只汇报摘要；
- 发现新需求时只记录候选。

它不一定有代码分支，也不应该亲自承担全部开发。

## 7.3 项目中控

负责：

- 统一读取任务台账；
- 排优先级；
- 判断能否并行；
- 判断依赖和冲突；
- 派给开发、审核、发布或观察 Agent；
- 只推送真正需要负责人决定的问题。

派单成功后立即恢复空闲。

## 7.4 开发 AI

负责：

- 按 GitHub 正式任务找代码；
- 修改代码；
- 补测试；
- 提交 PR；
- 修复审核问题；
- 在范围内更新工程文档。

开发入口：

```text
GitHub Task / Issue / PR
↓
AGENTS.md
↓
项目 AI 读取规则
↓
系统架构与风险规则
↓
对应模块文档
↓
真实代码
```

## 7.5 审核 / 发布 / 运维 AI

### 审核 AI

只审核，不代替原开发 AI 大范围修改代码。

### 发布 AI

只在明确授权后执行发布。

### 运维 / 观察 AI

定期检查：

- 账号池可用数量异常；
- 账号状态异常；
- 自动任务失败；
- Agent 卡住；
- PR 检查失败；
- 任务长时间没有进展；
- 线上服务异常。

无异常时不打扰负责人。

---

# 8. 一个长任务应该怎么运行

## 8.1 长聊天没有问题，但不能只依赖聊天历史

例如“账号分配与回收”聊很久没有关系。

每完成一个阶段，任务协调窗口做一次：

```text
本轮收口
```

然后更新：

```text
任务台账/账号分配与回收.md
```

## 8.2 长聊天可以换 Agent，但不要换任务

例如：

```text
账号分配调研会话
↓
账号分配需求收敛会话
↓
账号分配开发协调会话
↓
账号分配上线验证会话
```

它们仍然属于同一个任务和同一个任务文件。

## 8.3 新 Agent 如何接班

新 Agent 优先读取：

```text
任务台账/该任务.md
+
GitHub Issue / PR
+
本轮唯一下一步
```

不需要重新阅读全部历史聊天。

---

# 9. 多任务并行怎么做

项目可以同时存在多个活跃任务，但真正同时大规模改代码的任务要受控。

推荐按风险和模块拆跑道：

| 跑道 | 示例 | 是否写代码 |
|---|---|---:|
| 核心账号池 | 账号录入、状态、生命周期 | 是，建议最多 1 个核心改动同时进行 |
| 调度能力 | 分配、回收、轮换、并发控制 | 是或调查 |
| 内部权限 | 角色、权限、敏感操作控制 | 是 |
| 管理后台 | 内部管理页面、筛选、状态展示 | 是 |
| 稳定性 | 异常恢复、监控、告警 | 是或只读验证 |
| Review / 验证 | 独立审核、上线验证 | 通常只读 |

建议：

```text
活跃任务：4～5 个
同时大规模写代码：2～3 个
同一个任务的主要写入者：1 个
```

中控判断能否并行时只看三件事：

```text
是否修改同一个核心模块？
是否依赖另一个任务的结果？
是否可以单独验收？
```

---

# 10. 风险流程

不是所有功能都要走一样重的流程。

## 🟢 低风险

例如：

- 文案；
- 样式；
- 文档；
- 只读状态展示；
- 简单测试；
- 不影响账号分配逻辑的小后台调整。

流程：

```text
开始 → 开发 → 检查 → 审核 → 合并 → 汇报
```

## 🟡 普通风险

例如：

- 普通管理后台功能；
- 账号筛选；
- 状态同步；
- 普通自动化任务；
- 非核心日志与监控；
- 一般账号分配辅助逻辑。

负责人批准一次后：

```text
调查 → 开发 → 审核 → 修复 → 再审核 → 合并
```

范围变化或任务冲突时再找负责人。

## 🔴 高风险

例如：

- 登录与会话；
- 账号凭证；
- 权限模型；
- 自动账号分配核心逻辑；
- 会影响大量账号的批量操作；
- 数据库结构；
- 生产配置；
- 密钥或 Secret 管理；
- 可能导致账号大面积不可用的改动。

主要保留三个门：

```text
批准开发
↓
开发与审核内部循环
↓
批准合并 / 发布
```

如果合并会自动上线，必须在合并前明确说明。

---

# 11. 账号与敏感信息的额外规则

这一部分是 gpt-auto 必须单独强调的。

## 11.1 不在任务文档保存敏感凭证

Obsidian、GitHub Issue、PR 描述和普通聊天记录中不要长期保存：

- 明文密码；
- Cookie；
- Session Token；
- API Key；
- Secret；
- 可直接登录账号的完整凭证。

任务文档只记录：

```text
凭证存放在哪里
由什么角色管理
当前状态是否正常
需要什么操作
```

实际敏感信息应进入公司批准的 Secret / 凭证管理位置。

## 11.2 动态账号状态必须实时确认

以下内容不能只相信历史笔记：

- 账号当前是否可用；
- 登录是否仍有效；
- 当前配额或容量；
- 当前占用情况；
- 当前权限；
- 当前生产版本；
- 当前 PR / CI 状态。

执行操作前重新读取真实来源。

---

# 12. 产品助理到中控的流转

## 产品助理阶段

```text
随便聊需求
↓
本轮收口预览
↓
负责人确认
↓
写入正式需求
↓
标记“待中控安排”
```

## 中控阶段

```text
读取待安排需求
↓
读取相关任务台账
↓
结合 GitHub 当前状态
↓
判断优先级、并行、冲突和风险
↓
建立或更新正式 GitHub 任务
↓
派给执行 Agent
↓
立即恢复空闲
```

## 负责人不再做什么

不再：

```text
产品助理说完
→ 复制几页给中控
→ 中控说完
→ 再复制给开发
→ 开发说完
→ 再复制给审核
```

---

# 13. 任务过程中发现新需求怎么办

例如在“账号异常恢复”开发过程中发现：

> 可能还需要增加账号分组策略。

当前开发 Agent 不能顺手做。

正确流程：

```text
发现新需求
↓
记录到“新发现但不属于本任务的需求”
↓
任务协调窗口形成候选需求
↓
产品助理或中控判断
↓
决定现在做、排队或暂缓
```

---

# 14. 任务卡住后的主动上报

目标不是让负责人自己进入每个窗口找问题。

正确流程：

```text
任务卡住
↓
立即停止扩大工作
↓
更新自己的任务文件
↓
标记 needs_owner_decision: true
↓
通知项目中控
↓
中控放入【需要负责人决定】
```

中控通知格式：

```text
【需要你决定】

任务：
发生了什么：
影响什么：
选项 A：
选项 B：
推荐：
如果暂时不决定会怎样：
```

---

# 15. 可复制话术：产品助理

## 15.1 产品助理初始化

```text
你是我的 gpt-auto 产品助理。

这个项目是公司内部使用的 GPT 账号池项目。

这里是我长期讨论账号池、账号分配、账号状态、内部权限、管理后台、自动化、稳定性和使用流程的地方。

当前明确不做：
广告、支付、积分、订阅、定价、营销增长、公开用户注册等商业化功能。

我可能只有一个模糊想法，也可能反复改变方案。

你负责：

1. 帮我把零散想法合并成少数几个清楚方向；
2. 主动推荐最值得做的 1～3 件事；
3. 区分“想法、需要研究、准备好了、建议现在做、暂时不做”；
4. 明确第一版范围和明确不做的范围；
5. 讨论清楚后输出本轮收口；
6. 经我确认后写入 Obsidian 的“产品助理”区域；
7. 为项目中控准备短任务摘要。

你不负责：

- 修改代码；
- 开发；
- 审核；
- 合并或发布；
- 修改项目中控的执行状态；
- 把每一句聊天都写进 Obsidian；
- 擅自增加广告、支付、积分或其他商业化功能。

始终使用简单中文。
```

## 15.2 讨论新想法

```text
我有一个 gpt-auto 新想法：

【写想法】

现在先陪我想清楚，不开发，也不写文件。

请告诉我：

1. 我真正想解决什么；
2. 谁会在公司内部使用；
3. 为什么现在值得或不值得做；
4. 能不能和现有需求合并；
5. 你最推荐哪个方案；
6. 第一版最少做什么；
7. 第一版明确不做什么；
8. 可能影响账号池、权限或稳定性的地方；
9. 还需要我决定什么。

不要一次给我太多选择。
```

## 15.3 本轮收口

```text
请对刚才讨论做一次本轮收口。

只回答：

1. 已经确认了什么；
2. 第一版做什么；
3. 第一版不做什么；
4. 还没确认什么；
5. 建议保存到哪个文件；
6. 是否建议交给中控；
7. 现在需要我决定什么。

本次只预览，不修改文件。
```

## 15.4 写成正式需求

```text
这个方案定下来。

请先预览准备写入的内容。

目标文件：

产品助理/需求/【需求名称】.md

只保存：

- 要解决什么；
- 谁在内部使用；
- 为什么现在做；
- 第一版做什么；
- 第一版不做什么；
- 怎么算完成；
- 已确认决定；
- 还没确认；
- 与其他任务关系；
- 风险等级；
- 当前状态；
- 最后更新时间。

不要复制完整聊天。
等我确认后再写。
```

## 15.5 交给中控

```text
交给中控安排。

请先确认正式需求文件已经完整。

然后只在：

产品助理/03-待中控安排.md

加入一条简短记录：

- 需求名称；
- 为什么做；
- 优先级；
- 风险等级；
- 对应需求文件；
- 与现有任务关系；
- 是否需要我决定。

不要直接通知开发 Agent，不要修改中控文件。
```

---

# 16. 可复制话术：任务协调窗口

## 16.1 创建一个长期任务协调窗口

```text
你是 gpt-auto【任务名称】的长期任务协调 AI。

这个窗口可以长期沟通，但只负责这一件事。

你负责：

1. 保存这个任务的目标、范围、决定、进度和阻塞；
2. 维护唯一任务文件：
   任务台账/【任务名称】.md
3. 每次阶段变化时更新任务文件；
4. 读取与本任务有关的 GitHub Issue / PR 当前状态；
5. 向项目中控只返回简短摘要；
6. 发现新需求时只记录候选，不顺手开发；
7. 涉及账号状态、权限、配额、版本等动态事实时重新核验。

你不负责：

- 修改其他任务文件；
- 修改项目中控文件；
- 直接代替所有开发、审核和发布角色；
- 扩大任务范围；
- 把完整聊天复制进 Obsidian；
- 把明文账号凭证写入普通文档。

现在先读取现有任务文件和 GitHub 入口，用简单中文告诉我：
做什么、做到哪、还差什么、下一步是什么、是否需要我决定。
```

## 16.2 本轮收口

```text
请对本任务做一次本轮收口。

先只输出预览：

1. 本轮完成了什么；
2. 当前确定了什么；
3. 当前还没确定什么；
4. 现在为什么停着；
5. 下一步唯一动作；
6. 是否需要负责人决定；
7. 需要更新任务文件的哪些部分；
8. 是否发现不属于本任务的新需求；
9. 是否存在账号、权限、版本等动态事实需要重新核验。

不要立即写文件。
```

## 16.3 确认更新自己的任务文件

```text
确认更新。

本次只允许更新：

任务台账/【任务名称】.md

不要修改其他任务、产品助理或项目中控文件。

更新后只告诉我：

1. 改了什么；
2. 当前状态；
3. 下一步；
4. 是否需要中控；
5. 是否需要我决定。
```

## 16.4 任务发现新需求

```text
这个新发现不属于当前任务范围。

请不要开发。

只在当前任务文件的：

“新发现但不属于本任务的需求”

中记录：

- 新需求名称；
- 为什么发现；
- 对当前任务是否有影响；
- 建议现在做、排队还是暂缓。

然后交给项目中控或产品助理处理。
```

---

# 17. 可复制话术：项目中控

## 17.1 项目中控初始化

```text
你是 gpt-auto 项目中控。

这是公司内部 GPT 账号池项目。

当前项目不包含广告、支付、积分、订阅、定价、营销增长或公开用户注册功能。

你负责统一读取各任务文件、GitHub 当前状态和当前 Agent 状态。

你只负责：

1. 排优先级；
2. 判断哪些任务可以并行；
3. 判断任务依赖和冲突；
4. 判断风险等级；
5. 派发开发、审核、发布或观察任务；
6. 收集简短结果；
7. 只把真正需要负责人决定的事情告诉我；
8. 派单后立即恢复空闲。

你不负责：

- 长时间产品脑暴；
- 亲自开发；
- 亲自审核；
- 亲自等待 Worker 做完；
- 把完整日志发给我；
- 修改每个任务自己的文件；
- 擅自把范围扩展到商业化功能。

我问“项目怎么样”时，只显示：

【需要我决定】
【正在进行】
【建议接下来开始】
【等待】
【可以收尾】

始终使用简单中文。
```

## 17.2 读取共享任务台账

```text
请读取：

- 产品助理/03-待中控安排.md
- 任务台账/ 下最近更新的任务文件
- 项目中控/需要负责人决定.md
- 相关 GitHub Issue / PR 的实时状态

不要扫描整个 Obsidian。

请告诉我：

1. 哪些任务正在进行；
2. 哪些卡住；
3. 哪些可以同时进行；
4. 哪些存在冲突；
5. 哪些建议接下来开始；
6. 哪些需要我决定；
7. 哪些已经存在开发任务，不要重复创建；
8. 哪些可以收尾；
9. 哪些动态事实必须先重新核验。

本次只汇总和安排，不亲自执行。
```

## 17.3 派单后立即恢复空闲

```text
这次你只负责派单。

请：

1. 找到正确的任务 Agent；
2. 把任务说明交给它；
3. 确认对方已经收到；
4. 立即回到这里；
5. 不等待对方完成；
6. 不自己代替它执行。

只告诉我：

- 已交给哪个窗口；
- 对方是否收到；
- 中控是否已经恢复空闲。

如果无法自动派单，直接告诉我应该手动打开哪个窗口。
不要研究复杂替代方案。
```

## 17.4 项目状态简报

```text
项目现在怎么样？

只用简单中文告诉我：

【需要我决定】
只放真正需要我做决定的事情。

【正在进行】
每个任务最多两句话。

【建议接下来开始】
最多 1～3 项。

【等待】
说明在等什么。

【可以收尾】
说明为什么可以收尾。

不要执行任务。
```

---

# 18. 可复制话术：开发和审核 AI

## 18.1 开发 AI 启动

```text
你负责这个正式 gpt-auto 开发任务。

开始前必须：

1. 读取 GitHub 正式任务 / Issue / PR；
2. 读取仓库根目录 AGENTS.md；
3. 读取项目 AI 读取规则；
4. 按规则读取系统架构、风险分级和命中的模块文档；
5. 再定位真实代码；
6. 检查是否与其他正在开发的任务冲突。

当前任务：

【粘贴 GitHub 正式任务】

默认禁止：

- 扩大范围；
- 顺手修其他问题；
- 修改其他任务分支；
- 跳过审核；
- 未授权合并或上线；
- 把明文密码、Cookie、Token、API Key 或 Secret 写入代码和普通文档；
- 未经授权执行高风险账号批量操作。

先只输出：

1. 你理解的目标；
2. 本次范围；
3. 本次不做什么；
4. 需要读取哪些模块；
5. 可能和哪个任务冲突；
6. 风险等级；
7. 是否可以安全开始。

未确认前不要修改代码。
```

## 18.2 开发窗口用人话汇报

```text
先不要继续工作。

请用完全不懂技术的人也能看懂的话告诉我：

1. 你负责什么？
2. 原来有什么问题？
3. 已经做了什么？
4. 怎么确认基本正确？
5. 还没做什么？
6. 现在为什么停着？
7. 需要我决定什么？
8. 这个窗口现在能不能关闭？

不要执行新操作。
```

## 18.3 审核 AI

```text
你只负责独立审核，不修改代码。

项目：gpt-auto
任务：【任务名称】
GitHub PR：【PR 编号】
必须审核的版本：【版本编号】

开始前先确认版本一致。
如果不一致，立即停止。

请检查：

1. 是否满足任务目标；
2. 是否超出范围；
3. 是否错误加入广告、支付、积分或其他范围外能力；
4. 以前的问题是否修复；
5. 是否有新的必须修复问题；
6. 是否影响账号、权限、调度或生产稳定性；
7. 是否存在敏感凭证进入代码、日志或普通文档的风险；
8. 自动检查通过后是否仍有明显风险。

最终只给：

- 通过
或
- 需要继续修改

如果需要修改，只列最重要的 1～3 个问题，交回原开发窗口。

不要合并、不要上线、不要执行真实高风险账号操作。
```

---

# 19. 可复制话术：任务卡住和主动提醒

## 19.1 Worker 卡住

```text
当前任务已卡住，请立即停止扩展工作。

请更新本任务自己的任务文件，并输出：

【需要负责人决定】

任务：
做到哪一步：
为什么卡住：
已经保存了什么：
是否有丢失风险：
是否涉及账号可用性或生产风险：
选项 A：
选项 B：
你的推荐：
如果暂时不决定会有什么影响：

然后通知项目中控。
不要自己扩大处理范围。
```

## 19.2 中控扫描卡点

```text
请检查所有活跃任务文件和 GitHub 当前状态，找出：

- 标记需要负责人决定；
- 长时间没有更新；
- PR 检查失败；
- 审核不通过；
- 与其他任务冲突；
- 使用了错误需求版本；
- 等待上线或发布方式确认；
- 账号池可用性异常；
- 权限或凭证处理存在风险。

只把真正需要我处理的事项放到：

【需要我决定】

每项最多写：

1. 发生了什么；
2. 影响什么；
3. 选项；
4. 推荐；
5. 不处理的后果。

不要继续执行任务。
```

---

# 20. 可复制话术：任务收尾

## 20.1 任务协调窗口收尾

```text
这个任务准备收尾。

请先生成收尾预览：

1. 最终解决了什么；
2. 没有解决什么；
3. GitHub Issue / PR / 最终版本；
4. 审核和测试结果；
5. 是否已经合并；
6. 是否已经上线；
7. 上线验证结果；
8. 已知剩余风险；
9. 新发现的后续候选需求；
10. 哪些内容值得长期写入 Obsidian；
11. 是否涉及账号规则、权限规则或 SOP 更新；
12. 当前窗口能否安全关闭。

不要立即归档或删除。
```

## 20.2 写长期记忆

```text
确认写入长期记忆。

只保存：

- 为什么做；
- 最终结果；
- 已确认事实；
- 重要决定及原因；
- 账号规则；
- 权限规则；
- 关键数据口径；
- GitHub 交付证据；
- 剩余风险；
- 后续需求；
- 需要更新的 SOP。

不要复制完整聊天、日志、代码差异或敏感凭证。
```

## 20.3 清理窗口前

```text
请只读检查这个任务是否能安全关闭。

回答：

1. 是否还有未保存内容；
2. 是否还有未上传版本；
3. PR 是否已完成；
4. 重要结论是否已写入 GitHub 或 Obsidian；
5. 是否有其他任务依赖；
6. 最终版本是否已记录；
7. 是否仍有生产或账号池风险；
8. 现在能否关闭；
9. 不能关闭的唯一原因是什么。

不要执行删除。
```

---

# 21. 三种 gpt-auto 真实工作场景示例

## 21.1 账号分配与回收

```text
产品助理
→ 保存为什么做、第一版分配规则和非范围

任务协调窗口
→ 维护 账号分配与回收.md

项目中控
→ 检查是否与账号池核心状态模块冲突
→ 建立唯一 GitHub 正式任务

开发 AI
→ 只按正式需求和 GitHub 工程任务开发

审核 AI
→ 独立确认分配逻辑没有越权、超范围或破坏现有账号状态
```

## 21.2 账号异常恢复

```text
异常恢复任务协调窗口
→ 保存当前问题、复现情况、影响范围和恢复方案

GitHub
→ 保存代码、PR、版本、测试和审核结果

项目中控
→ 如果存在大面积账号不可用风险，只向负责人推送真正需要决定的事项
```

## 21.3 内部权限

```text
权限任务协调窗口
→ 明确哪些角色可以看、改、分配或执行高风险操作

项目中控
→ 判断是否和管理后台、账号调度任务冲突

开发 AI
→ 按最新权限需求开发

审核 AI
→ 重点检查越权路径、默认权限和敏感操作
```

---

# 22. 非技术负责人每日使用方式

## 早上

只问中控：

```text
项目怎么样？
```

## 白天想到新东西

找产品助理：

```text
我有个 gpt-auto 的想法……
```

## 对某个任务有长期讨论

进入该任务协调窗口，不直接进入代码开发窗口。

## 一轮讨论结束

说：

```text
请做本轮收口，先不要写文件。
```

## 想保存

说：

```text
确认更新自己的任务文件。
```

## 想排期

说：

```text
交给中控安排。
```

## 想知道某个开发做了什么

说：

```text
请用不懂技术的人也能听懂的话汇报。
```

---

# 23. M1～M4 落地顺序

## M1：产品助理记忆

```text
产品助理能聊需求
↓
能收口
↓
能写自己的 Obsidian 区域
```

## M2：共享任务台账

```text
为每个长期任务建立唯一任务文件
↓
每个任务协调窗口只维护自己的文件
```

## M3：中控统一读取

```text
中控读取任务文件和 GitHub 当前状态
↓
判断排期、并行、冲突、风险和需决策事项
```

## M4：主动提醒

```text
Worker 卡住或监控发现异常
↓
更新任务文件
↓
通知中控
↓
中控只把真正需要负责人处理的事项推送出来
```

---

# 24. 跨工具项目记忆

即使以后不用 Orca，也可以继续维护 gpt-auto。

## 新产品 / 需求 AI

读取：

```text
AI项目记忆入口.md
当前项目目标.md
产品助理需求文件
相关任务台账文件
```

## 新开发 AI

读取：

```text
GitHub 正式任务
AGENTS.md
项目 AI 读取规则
系统架构
风险规则
模块文档
真实代码
```

## 新项目中控 AI

读取：

```text
当前项目目标
产品助理/03-待中控安排.md
任务台账/
项目中控/
GitHub 当前 Issue / PR / CI
```

---

# 25. AI 项目记忆入口模板

```markdown
# gpt-auto AI 项目记忆入口

## 项目是什么

gpt-auto 是公司内部使用的 GPT 账号池及相关管理项目。

## 明确不做

- 广告
- 支付
- 积分
- 订阅
- 定价 / 套餐
- 营销增长
- 公开用户注册

## 当前项目目标

1.
2.
3.

## 当前最重要任务

1.
2.
3.
4.
5.

## 当前主要阻塞

1.
2.

## 最近确认的产品决定

1.
2.
3.

## 当前核心账号规则

1.
2.
3.

## 当前核心权限规则

1.
2.
3.

## AI 应该怎样支持负责人

- 使用简单中文；
- 主动收敛，不一次丢太多方案给负责人；
- 不让负责人成为窗口之间的传话人；
- 只推真正需要负责人决定的事项；
- 动态事实实时核验；
- 不确定时明确说明需要读取什么；
- 不把敏感账号凭证写入普通任务文档；
- 不擅自扩展到商业化功能。

## 工程入口

- GitHub `AGENTS.md`
- 项目 AI 读取规则
- 系统架构说明
- 风险分级与影响矩阵

## 产品记忆入口

- 产品助理/
- 任务台账/
- 产品决策/

## 重要提醒

本页只提供快速入口，不代表当前 PR、部署、账号可用性、配额或线上运行状态。
```

---

# 26. 最终验收清单

```text
[ ] 项目名称已经统一为 gpt-auto
[ ] 项目定位明确为公司内部 GPT 账号池
[ ] 文档不再包含广告、支付、积分等商业化需求
[ ] 每个复杂任务有唯一任务文件
[ ] 每个任务文件有唯一主要维护者
[ ] 产品助理只维护自己的区域
[ ] 中控主要读取、排期和派单，不抢长期任务
[ ] 开发 AI 以 GitHub 为工程入口
[ ] 负责人不用复制完整聊天
[ ] 中控派单后立即恢复空闲
[ ] 多个任务可以并行推进
[ ] 同时大规模写代码的任务数量受控
[ ] Worker 卡住会主动上报
[ ] 新需求不会被开发 AI 顺手实现
[ ] 高风险账号 / 权限 / 凭证改动有独立审核
[ ] 敏感凭证不进入普通文档和代码
[ ] 动态账号状态会在执行前重新核验
[ ] 任务结束后重要结论会长期保存
[ ] 换 ChatGPT、Codex、Claude 或不用 Orca 仍能恢复项目
```

---

# 27. 所有 AI 都要记住

```text
这是 gpt-auto，公司内部 GPT 账号池项目。

一件事一个文件。
一份文件一个主要维护者。
中控统一读取、排期和派单，不亲自做长任务。
开发以 GitHub 为准，不依赖私人聊天历史。
Orca 是执行现场，不是唯一项目记忆。
账号、权限、版本、配额等动态事实必须实时核验。
敏感凭证不能进入普通文档和代码。
新需求不能顺手扩大开发范围。
广告、支付、积分和其他商业化功能当前不属于本项目。
负责人只处理真正需要负责人决定的事情。
```
