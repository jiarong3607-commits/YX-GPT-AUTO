---
task_name: "结课作业图片生成原型"
status: "高风险第一批本地改动已落地，未正式 Git 合并，未发布，未上线"
priority: "中"
risk: "高"
owner_window: "产品助理"
last_updated: "2026-09-06"
needs_owner_decision: true
github_issue: ""
github_pr: ""
current_version: "高风险第一批本地版本"
---

# 结课作业图片生成原型

## 要解决什么

为 gpt-auto 做一个内部结课作业最小演示原型，让内部使用者可以在网页选择车队，进入 GPT 图片生成聊天页，输入提示词后生成并展示图片。

## 设计网页网址

- 已购买主域名：`yunxi.io`；
- 后续上线二级域名以 `temu.yunxi.io` 为准。

## 谁在内部使用

- 公司内部项目负责人；
- 内部测试人员；
- 参与结课作业演示的人。

## 第一版做什么

- 网页选择车队；
- 进入 GPT 图片生成聊天页；
- 输入提示词；
- 生成并展示图片；
- 显示侧边栏运行状态；
- 做后端参数校验；
- 记录非敏感运行日志；
- 支持 `DEMO_IMAGE_MODE` 演示模式，便于无真实 API Key 时验证原型。

## 第一版不做什么

- 不做广告、支付、积分、订阅、定价、公开注册等商业化功能；
- 不接入真实账号凭证、生产 Secret、登录、数据库或自动调度；
- 不做复杂账号自动分配、回收、轮换规则；
- 不做完整管理后台；
- 不做多角色权限体系；
- 不做生产级监控、审计和异常恢复；
- 不保存明文账号密码、Cookie、Token、API Key 或 Secret。

## 怎么算完成

- 可以完成“网页选择车队 → 进入 GPT 图片生成聊天页 → 输入提示词 → 生成并展示图片”的最小演示流程；
- 开发 AI 自测通过：语法检查通过、本地静态资源返回 200、无 API Key 时有明确错误、演示模式接口返回 200；
- 审核 AI 已审核通过，结论为“通过”。

## 已确认决定

- 第一版只做内部结课作业最小演示流程，不做商业化功能；
- 设计网页网址是 `temu.yunxi.io`；
- 当前已通过开发自测和审核 AI 审核，结论为“通过”；
- 当前未正式 Git 合并、未发布、未上线；
- 演示模式可在无真实 API Key 时用于原型验证；
- 负责人已批准开始下一阶段高风险开发；
- 负责人已批准合并第一批改动；
- 开发 AI 检查后确认当前目录不是 Git 仓库，无法执行正式分支合并；
- 不能伪造提交号、分支名或 PR 状态；
- 一旦接入真实账号凭证、生产 Secret、登录、数据库或自动调度，必须升级为高风险流程。

## 高风险第一批合并/版本确认

- 当前状态：高风险第一批改动已落在本地项目目录 `D:\OrcaWorkspace\gpt-auto`；
- 当前限制：当前目录不是 Git 仓库，无法执行正式分支合并；
- 版本记录：不能伪造提交号、分支名或 PR 状态；
- 开发验证：`node --check server.mjs` 通过，`node --check public/app.js` 通过；
- 敏感信息扫描：未发现真实 `OPENAI_API_KEY`、真实 `INTERNAL_ACCESS_CODE`、Cookie、Token、Secret 或明文凭证；
- 运行数据检查：未发现运行数据文件残留；
- 发布状态：未发布、未上线。

## 还没确认

- 是否发布或上线；
- 生产 Secret 存放位置；
- 访问码规则；
- 部署环境；
- 数据库或运行记录方式；
- 下一阶段是否继续接入真实账号或生产图片生成能力。

## 与其他任务关系

- 依赖：如果下一步上线，需要负责人另行批准生产 Secret 存放位置、访问码规则、部署环境和数据库/记录方式；
- 依赖：如果下一阶段接入真实账号、生产 Secret、登录、数据库或自动调度，需要项目中控按高风险流程继续安排；
- 可能冲突：可能与账号池核心管理、账号分配与回收、内部权限、生产配置等任务发生冲突；
- 可以并行：产品文档沉淀、上线前决策、部署方案确认可以并行，但发布/上线必须单独批准。

## 风险等级

高风险。

原因：下一阶段涉及或可能涉及真实账号/API Key、登录、数据库、生产 Secret、访问码、部署环境或自动调度。虽然当前扫描未发现真实敏感凭证，也未发现运行数据文件残留，但上线前仍必须重新确认生产 Secret、访问控制和数据记录方式。

## 当前状态

高风险第一批改动已落在本地项目目录 `D:\OrcaWorkspace\gpt-auto`。负责人已批准合并第一批改动，但当前目录不是 Git 仓库，无法执行正式分支合并，也不能伪造提交号、分支名或 PR 状态。开发验证通过，敏感信息扫描未发现真实凭证或明文敏感信息，未发现运行数据文件残留；当前未发布、未上线。

## 下一步

如果要上线，需要负责人另行批准生产 Secret 存放位置、访问码规则、部署环境和数据库/记录方式。

## 最后更新时间

2026-09-06
